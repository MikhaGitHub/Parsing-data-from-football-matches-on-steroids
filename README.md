# This project is related to data parsing using selenium library.
First, the code switches to a specific tab with sports.
Next, all matches of the main and additional statistics are parsed in the cycle.
